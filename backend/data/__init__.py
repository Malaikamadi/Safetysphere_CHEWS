"""Static reference datasets used by the CHEWS backend (e.g. Sierra
Leone administrative geography, flood-prone communities, district
weather climatologies). Kept separate from `models/` so the data is
usable by routers and services without pulling in ML code paths."""
